# Pipeline runtime deps (chạy trên Kaggle notebook — python3 chính)
playwright
ffmpeg-python
edge-tts
vieneu
transformers==4.57.6

# TTS engine "vieneu" chạy trong venv riêng (xem build_template.py SETUP cell) —
# cài vào tts_env với numpy<2.3, scipy, transformers, safetensors, gradio,
# huggingface_hub, kaldi-native-fbank, librosa, onnxruntime, PyYAML, sea-g2p,
# soundfile, soxr, tokenizers (đã có trong cell). Không cần ở đây.